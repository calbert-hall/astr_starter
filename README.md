# APPLITOOLS SIMPLE TEST RUNNER - Starter Project

1. Clone the repo
2. Set APPLITOOLS_API_KEY and APPLITOOLS_SERVER_URL
3. Run the test with `java -jar ApplitoolsSimpleTestRunner.jar ASTRFiles/job.xml`
